on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v3.1.1
      with:
        ftp-server: 66.225.201.137
        ftp-username: siiecro1@siiecro.com.mx
        ftp-password: ${{ secrets.FTP_ACCESO }}
        protocol: ftps
 
